name: CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  push:
    branches: [ main, master ]
    tags: [ "v*" ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          set -euxo pipefail
          python -m pip install --upgrade pip
          pip install -e ".[test,dev]"

      - name: Run tests
        run: |
          set -euxo pipefail
          mkdir -p .ci-logs
          pytest -vv --showlocals 2>&1 | tee .ci-logs/pytest.txt

      - name: Ruff (lint)
        run: |
          set -euxo pipefail
          ruff check . 2>&1 | tee .ci-logs/ruff_check.txt

      - name: Ruff (format)
        run: |
          set -euxo pipefail
          ruff format --check . 2>&1 | tee .ci-logs/ruff_format.txt

      - name: Mypy
        run: |
          set -euxo pipefail
          mypy . 2>&1 | tee .ci-logs/mypy.txt

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-logs-tests
          path: .ci-logs/*

  build:
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[gui]"
          pip install pyinstaller

      - name: Build GUI app
        run: |
          pyinstaller --noconfirm --onefile --windowed --name AssetValidator pipeline/gui/app.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AssetValidator-${{ matrix.os }}
          path: dist/*

      - name: Create GitHub Release
        if: matrix.os == 'ubuntu-latest'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*

