{
  "tag": "v2.8.4",
  "version": "2.8.4",
  "notes": "**Fix: Open report in browser (ERR_EMPTY_RESPONSE on Windows)**\n\n- Open the self-contained HTML report via `file://` instead of a local HTTP server\n- Windows fallback uses `os.startfile` when `webbrowser.open` fails\n- GUI opens the report from the main thread with the same file-based path",
  "windows_url": "https://github.com/daniilcg/AssetValidator/releases/download/v2.8.4/AssetValidator-Setup.exe",
  "linux_url": "https://github.com/daniilcg/AssetValidator/releases/download/v2.8.4/AssetValidator"
}
