{
  "tag": "v2.8.3",
  "version": "2.8.3",
  "notes": "**Fix: Open report in browser (ERR_EMPTY_RESPONSE)**\n\n- Wait until the local report HTTP server responds before opening the browser\n- Keep server instances alive; use a dedicated handler class on Windows\n- Open the browser from the GUI main thread after validation completes\n- Enabling \"Open report in browser\" auto-generates the HTML report if needed",
  "windows_url": "https://github.com/daniilcg/AssetValidator/releases/download/v2.8.3/AssetValidator-Setup.exe",
  "linux_url": "https://github.com/daniilcg/AssetValidator/releases/download/v2.8.3/AssetValidator"
}
